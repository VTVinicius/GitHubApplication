plugins {
    id 'com.google.devtools.ksp' version '1.7.20-1.0.7'
}


apply plugin: 'kotlin'

dependencies {
    domainDependencies.each { add(it.configuration, it.dependency, it.options) }
}

repositories {
    google()
    mavenCentral()
}